# User Management System
A User Management System (UMS) in Core PHP + PostgreSQL(or MySQL) is a classic backend project that demonstrates authentication, authorization, and CRUD operations

## Features to Include
- User Registration
- Login / Logout
- Password Hashing
- Role-based Access (Admin/User)
- CRUD (Create, Read, Update, Delete users)
- Session handling